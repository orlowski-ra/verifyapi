PORT=3000
NODE_ENV=development

# Optional: Enable SMTP verification (slower but more accurate)
ENABLE_SMTP_CHECK=false

# For production deployment on Railway/Render
# These are set automatically by the platform
# RAILWAY_STATIC_URL=
# RENDER_EXTERNAL_URL=